[http]
delay_ms = 100
max_concurrent = 10
timeout_secs = 30
retries = 3
# proxy = "socks5://127.0.0.1:1080"

[output]
format = "markdown"
compact = true
strip_stopwords = false

[server]
host = "0.0.0.0"
port = 3000
